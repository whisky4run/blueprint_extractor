# Blueprint Extractor — 技術アプローチサマリー

## 1. システム全体像

```
ブラウザ (React SPA)
    │  PDF アップロード
    ▼
FastAPI バックエンド
    ├─ [1] PDF → PNG 変換      (pymupdf)
    ├─ [2] 候補領域検出         (OpenCV)
    ├─ [3] パーツ分類 + タイトル (Azure OpenAI GPT-4o)
    └─ [4] PNG 切り出し + ZIP  (Pillow)
    │  セッションID + BBox一覧
    ▼
ブラウザ (プレビュー表示 → ZIP ダウンロード)
```

---

## 2. コアアーキテクチャの決定事項

### 2-1. AI + OpenCV のハイブリッド方式

| 役割 | 担当 | 理由 |
|------|------|------|
| **どこにパーツがあるか（座標）** | OpenCV | ピクセル精度の座標推定はLLMに不得手。罫線検出は古典的CVが確実 |
| **何がパーツか（分類）＋タイトル文字列** | GPT-4o | 非定型レイアウトの意味理解・OCR的タスクはLLMが得意 |

当初はAIに座標まで推定させる方式（ピクセル値でBBoxを返させる）を取ったが、以下の理由で精度が低かった：
- GPT-4oは画像をトークンに変換して処理するため、ピクセル座標の精度が保証されない
- 画像解像度（150dpi）とモデルが「見ている」解像度の乖離

### 2-2. OpenCV の罫線検出アルゴリズム

形態学的フィルタ（Morphological Open）を用いた検出：

```
グレースケール変換
    → 二値化（閾値200: 線=白, 背景=黒）
    → 水平カーネルで Open → 水平線のみ抽出
    → 垂直カーネルで Open → 垂直線のみ抽出
    → 射影（列/行方向の総和）でピーク座標を取得
    → 連続する線のペアで矩形候補を生成
```

カーネルサイズは画像の幅・高さに対して相対的（`MIN_LINE_SPAN_RATIO = 0.08`）にしており、A4〜A0 など異なる図面サイズに対応。

### 2-3. AIへの入力設計

AIには「番号付き赤枠を描いた画像 + 候補一覧テキスト」を渡し、**インデックスとタイトルだけ返させる**。

```
AI への入力:
  - 赤枠＋番号を描画した PNG（base64）
  - 候補一覧テキスト（[0]: x=.., y=.. 幅=.. 高さ=..）

AI からの出力:
  [{"index": 2, "title": "NHE II-450 SPOK S:1/10"}, ...]

座標は一切AIに推定させない。
```

---

## 3. 処理フロー詳細

### Step 1: PDF → PNG 変換
- ライブラリ: `pymupdf`（`fitz`）
- 解像度: 既定 **300 DPI**（`PDF_RENDER_DPI` で変更可能）
- 1ページ = 1 PNG ファイルとして `/tmp` に保存

### Step 2: OpenCV 候補領域検出（`cv_detector.py`）
- 最小線スパン: 画像短辺の **8%** 以上の線のみ採用（ハッチングなどのノイズを除去）
- 最小候補面積: 画像全体の **1.5%** 以上（小さな注記欄などを除去）
- 隣接する線のマージ: **12px** 以内は1本とみなす

### Step 3: AI 分類（`detector.py`）
- モデル: Azure OpenAI GPT-4o（Azure AI Foundry 経由）
- 温度: `0`（決定論的出力）
- プロンプトで除外条件を明示: 表題欄・空白領域・凡例のみの欄

### Step 4: PNG 切り出し + ZIP（`extractor.py`）
- ライブラリ: `Pillow`（`Image.crop`）
- ファイル名: タイトル文字列をそのまま使用（ファイル名に使えない文字は `_` に置換）
- ダウンロード形式: `zipfile` で一括 ZIP

---

## 4. セッション管理

- DB なし。プロセスメモリ上の Python dict に保持
- セッションデータ: `{session_id → {dir, parts, page_count}}`
- 一時ファイル: `/tmp/blueprint_extractor/{session_id}/` 以下
- スケールアウト非対応（MVP 割り切り）

---

## 5. 既知の限界・課題

| 課題 | 詳細 | 想定される対策 |
|------|------|----------------|
| **罫線が途切れている図面** | OpenCV が領域を検出できない | 線の膨張処理（dilate）を追加、またはエッジ検出ベースに変更 |
| **斜め線・非矩形パーツ** | 射影ベースの手法は直交グリッドを前提とする | Hough変換または輪郭ベースに変更 |
| **タイトルなしパーツ** | AI がタイトルを返せないため除外される | AIに「タイトル不明」でも返させるオプション化 |
| **高解像度 PDF でのメモリ** | 300dpi × A0 ではメモリ使用量が大きくなる | `PDF_RENDER_DPI` を下げる、またはDPIを動的調整（ページサイズ検出後） |
| **マルチプロセス非対応** | メモリ上セッションはプロセス共有不可 | Redis / ファイルベースセッションに移行 |
| **セッションの揮発** | サーバー再起動でセッション消滅 | 同上 |
| **列注記行の bbox 帰属問題** | 図面によっては、複数パーツ列の最上段に「Pタイル用」「磁器タイル用」などの**列共通注記行**が存在する。この行は AzureCU ではタイトルとして認識されないため、どのパーツの bbox にも含まれない。結果として、同行が**タイトルとして認識される隣列のパーツ（例：外枠▪内枠断面詳細図）との間に上端のずれ（97px 相当）が生じる**。ユーザーが注記行を各パーツ画像に含めたい場合、bbox 上端を1行分さかのぼる処理が必要。現状は bbox 座標としては正確であり、描画の構造を忠実に反映している。 | `_expand_parts_by_grid` の展開ロジックをタイトルセル上端から「隣接する注記小セル」まで遡る方向へ拡張。ただし列注記と別列パーツ上端を同一セルとみなすか否かの定義が難しく要議論。 |

---

## 6. 技術スタック一覧

| レイヤー | 技術 | バージョン |
|----------|------|----------|
| フロントエンド | React + TypeScript (Vite) | React 18 |
| バックエンド | Python + FastAPI | Python 3.12 |
| PDF 変換 | pymupdf | >=1.24 |
| 画像処理 | OpenCV (headless) + Pillow | >=4.10 / >=10.3 |
| AI | Azure OpenAI GPT-4o | API ver. 2024-02-01 |
| コンテナ | Docker Compose | - |

---

## 7. 今後の検討ポイント（壁打ち用）

- **DPI 設計**: AzureCU の安定性を優先した 300dpi とメモリ/速度のバランスをどう取るか？
- **CV パラメータのチューニング**: `MIN_LINE_SPAN_RATIO` などは図面の種類によって変わりうるか？
- **AI コスト**: 1ページごとに API 呼び出し → マルチページPDFでコストが増大。バッチ化の余地は？
- **セッション永続化**: MVP を超えてスケールさせるなら何が最小コストの対策か？
- **手動補正 UI**: CLAUDE.md では「将来対応」とされているが、CVの限界を補うためにどこまで優先するか？
