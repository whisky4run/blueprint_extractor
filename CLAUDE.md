# CLAUDE.md

## プロジェクト概要

建築図面PDFを入力とし、図面を構成するパーツ（ビュー）単位で画像切り出しを行うWebアプリケーション。

### 背景・課題

- 建築図面には複数のパーツ（平面図・断面図など）が1枚のPDFにまとめられている
- 図面のフォーマットは非定型（パーツの数・配置が図面ごとに異なる）
- 各パーツを個別のPNG画像として切り出したい

### パーツ検出の前提

- パーツ同士は**罫線**で区切られている
- 各パーツには**タイトル**（例: `NHE II-450 SPOK S:1/10`）が付いている
- タイトルと罫線を手がかりにパーツ領域を検出する
- 表題欄（会社名・仕様欄）は切り出し対象外とする

---

## 技術スタック

| レイヤー | 技術 |
|---|---|
| クラウド | Azure |
| AI | Azure AI Foundry |
| フロントエンド | React |
| バックエンド | Python / FastAPI |

---

## 機能要件

### 入力
- PDFファイルのみを対象とする

### 処理
- PDFをページ単位で画像化する
- 罫線とタイトルを手がかりにパーツ領域を自動検出する（AI活用）
- 各パーツをPNG形式で切り出す

### 出力
- ファイル形式：PNG
- ファイル名：パーツのタイトル文字列

### UI
- WebブラウザベースのSPA（React）
- PDFをアップロードすると、検出したパーツの切り出し範囲をプレビュー表示する
- プレビュー確認後、PNG一括ダウンロードができる
- ※ 手動での切り出し範囲調整機能は現時点でスコープ外（将来対応）

---

## 作業ルール

### 実装タスクの実行条件

**「実装して」という指示があった場合にのみ、実装を含む重いタスク（コード生成・ファイル作成・依存関係追加など）を実行すること。**

それ以外の場面（質問・要件確認・設計提案など）では、実装を行わず対話にとどめること。

### その他の指針

- 実装前に設計・方針をユーザーに提示し、確認を取ること
- 不明点があれば実装前に質問すること
- スコープ外の機能を勝手に追加しないこと
